# ─────────────────────────────────────────────────────────────
# 빌드 산출물 / 로컬 캐시
# ─────────────────────────────────────────────────────────────
build/
out/
bin/
.gradle/

# ─────────────────────────────────────────────────────────────
# VCS / IDE
# ─────────────────────────────────────────────────────────────
.git/
.gitattributes
.gitignore
.idea/
.vscode/
*.iml
*.ipr
*.iws

# ─────────────────────────────────────────────────────────────
# 이미지에 절대 들어가면 안 되는 것들
# ─────────────────────────────────────────────────────────────
# API 키 (ANTHROPIC_API_KEY / OPENAI_API_KEY)
.env

# JWT 검증용 공개키.
# 공개키라 유출 위험은 없지만, 이미지에 굽는 순간 키 교체 = 재빌드가 된다.
# dev/prod 는 k8s Secret 을 파일로 마운트해서 주입한다 (application-{dev,prod}.yaml 참고).
src/main/resources/keys/

# 로컬에서 생성된 도시 이미지 / 임시 파일
city-images/
tmp/

# ─────────────────────────────────────────────────────────────
# 런타임에 필요 없는 것들
# ─────────────────────────────────────────────────────────────
docs/
*.md
compose.yaml
Dockerfile
.dockerignore
k8s/
